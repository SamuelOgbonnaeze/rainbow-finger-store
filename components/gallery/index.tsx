"use client"

import {Tab} from "@headlessui/react"
import { Image } from "@/types";

interface GalleryProps {
    images: Image[];
}

const Gallery = () => {
    return (
        <div>
            kjd
        </div>
    )
}

export default Gallery;